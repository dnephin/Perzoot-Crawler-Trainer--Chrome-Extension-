Perzoot Crawler Trainer
Chrome Extension

This chrome extension is used to manually classify links on a job site.  These
classifications are then sent to the trainer_upload so they can be stored for
the crawlers classifier for the next crawl.
